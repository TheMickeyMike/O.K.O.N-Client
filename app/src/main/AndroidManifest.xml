<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roundstarstudio.maciej.okon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ui.activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ui.activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ui.activities.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ui.activities.RecyclerActivity"
            android:label="@string/title_activity_recycler"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ui.activities.recyclerViewPt.CardViewActivity"
            android:label="@string/title_activity_recycler"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ui.activities.recyclerViewLast.recActivity"
            android:label="@string/title_activity_rec"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.ui.activities.UserProfile"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
